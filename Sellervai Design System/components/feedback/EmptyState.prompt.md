Centered empty state for zero-data tables, inboxes, and product lists — keep it calm and give one clear next action.

```jsx
<EmptyState icon="messages-off" title="No conversations yet"
  action={<Button variant="primary" icon="plug-connected">Connect a channel</Button>}>
  Once you connect a platform, customer chats land here.
</EmptyState>
```
